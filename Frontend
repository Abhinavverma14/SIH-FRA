<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FRA Atlas — AI-powered WebGIS for Forest Rights Act</title>
  <style>
    /* Reset & base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #2c5f2d, #97bc62);
      color: #f0f0f0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem 1rem 4rem;
      overflow-x: hidden;
    }
    a {
      text-decoration: none;
      color: inherit;
    }

    /* Hero Section */
    .hero {
      max-width: 900px;
      text-align: center;
      margin-bottom: 3rem;
      position: relative;
      overflow: visible;
    }
    .hero-title {
      font-size: 2.8rem;
      font-weight: 900;
      line-height: 1.1;
      margin-bottom: 0.5rem;
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 1s ease forwards;
      animation-delay: 0.3s;
    }
    .hero-subtitle {
      font-size: 1.3rem;
      font-weight: 500;
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 1s ease forwards;
      animation-delay: 0.8s;
      color: #d9e6c2;
    }

    /* Background forest illustration (SVG) */
    .hero-bg-illustration {
      position: absolute;
      bottom: -20px;
      left: 50%;
      transform: translateX(-50%);
      width: 100%;
      max-width: 900px;
      opacity: 0.15;
      pointer-events: none;
      user-select: none;
      z-index: 0;
    }

    /* Quick Action Buttons */
    .quick-actions {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 4rem;
      flex-wrap: wrap;
      justify-content: center;
      z-index: 1;
    }
    .btn {
      background: rgba(255 255 255 / 0.15);
      border: 2px solid rgba(255 255 255 / 0.3);
      color: #f0f0f0;
      font-weight: 600;
      font-size: 1.1rem;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 0.6rem;
      transition:
        background-color 0.3s ease,
        border-color 0.3s ease,
        transform 0.3s ease,
        box-shadow 0.3s ease;
      user-select: none;
      box-shadow: 0 0 0 transparent;
    }
    .btn:hover,
    .btn:focus {
      background: rgba(255 255 255 / 0.35);
      border-color: #d9e6c2;
      transform: translateY(-3px);
      box-shadow: 0 8px 15px rgba(217, 230, 194, 0.4);
      outline: none;
    }
    .btn:active {
      transform: translateY(-1px);
      box-shadow: 0 4px 8px rgba(217, 230, 194, 0.3);
    }

    /* About Section */
    .about {
      max-width: 900px;
      background: rgba(255 255 255 / 0.1);
      border-radius: 12px;
      padding: 2rem 2.5rem;
      margin-bottom: 4rem;
      box-shadow: 0 0 15px rgba(0,0,0,0.15);
      color: #e6f0c9;
      line-height: 1.5;
      z-index: 1;
    }
    .about h2 {
      font-size: 2rem;
      margin-bottom: 1rem;
      font-weight: 700;
      text-align: center;
      color: #d9e6c2;
    }
    .about p {
      font-size: 1.1rem;
      margin-bottom: 1rem;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
    }
    .about-icons {
      display: flex;
      justify-content: center;
      gap: 2.5rem;
      font-size: 2.5rem;
      margin-top: 1rem;
      user-select: none;
    }
    .about-icons span {
      display: flex;
      flex-direction: column;
      align-items: center;
      font-weight: 600;
      font-size: 1.1rem;
      color: #c9d9a7;
      gap: 0.3rem;
    }

    /* Preview Cards */
    .preview-cards {
      max-width: 900px;
      display: flex;
      gap: 2rem;
      justify-content: center;
      flex-wrap: wrap;
      z-index: 1;
    }
    .card {
      background: rgba(255 255 255 / 0.1);
      border-radius: 12px;
      padding: 1.8rem 2rem;
      width: 260px;
      box-shadow: 0 0 15px rgba(0,0,0,0.15);
      color: #e6f0c9;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1rem;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: default;
      user-select: none;
    }
    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 12px 25px rgba(217, 230, 194, 0.5);
    }
    .card-icon {
      font-size: 4rem;
      color: #a8c66c;
      user-select: none;
    }
    .card-title {
      font-weight: 700;
      font-size: 1.3rem;
      text-align: center;
      color: #d9e6c2;
    }

    /* Animations */
    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Responsive */
    @media (max-width: 600px) {
      .hero-title {
        font-size: 2rem;
      }
      .hero-subtitle {
        font-size: 1rem;
      }
      .quick-actions {
        flex-direction: column;
        gap: 1rem;
      }
      .preview-cards {
        flex-direction: column;
        gap: 1.5rem;
      }
      .card {
        width: 100%;
        max-width: 320px;
      }
    }
  </style>
</head>
<body>
  <section class="hero" aria-label="Hero section with project title and subtitle">
    <h1 class="hero-title">FRA Atlas — AI-powered WebGIS for Forest Rights Act</h1>
    <p class="hero-subtitle">Visualize claims, monitor progress, and get AI insights for better decision-making.</p>
    <svg class="hero-bg-illustration" viewBox="0 0 900 150" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
      <path fill="#2c5f2d" d="M0 150 L50 100 L100 130 L150 90 L200 140 L250 110 L300 150 L350 120 L400 150 L450 110 L500 140 L550 100 L600 130 L650 90 L700 140 L750 110 L800 150 L850 120 L900 150 V0 H0 Z"/>
    </svg>
  </section>

  <nav class="quick-actions" aria-label="Quick action buttons">
    <a href="map.html" class="btn" role="button" tabindex="0" aria-label="Explore Atlas - go to Map page">🌐 Explore Atlas →</a>
    <a href="dashboard.html" class="btn" role="button" tabindex="0" aria-label="View Dashboard - go to Dashboard page">📊 View Dashboard →</a>
    <button class="btn" disabled aria-label="AI Insights - coming soon">🎯 AI Insights (Coming Soon)</button>
  </nav>

  <section class="about" aria-labelledby="about-title">
    <h2 id="about-title">About FRA Atlas</h2>
    <p>FRA Atlas is an AI-powered WebGIS platform designed to visualize forest rights claims, monitor implementation progress, and provide actionable AI insights for better decision-making.</p>
    <p>It plays a crucial role in monitoring the Forest Rights Act implementation across key states like Odisha, Madhya Pradesh, Telangana, and Tripura, helping stakeholders ensure transparency and effectiveness.</p>
    <div class="about-icons" aria-hidden="true">
      <span>🌍<br />Map</span>
      <span>📈<br />Dashboard</span>
      <span>🤖<br />AI</span>
    </div>
  </section>

  <section class="preview-cards" aria-label="Preview of other pages">
    <article class="card" tabindex="0" aria-describedby="map-desc">
      <div class="card-icon" aria-hidden="true">🗺</div>
      <h3 class="card-title">Interactive Map Preview</h3>
      <p id="map-desc" style="font-size:0.9rem; color:#c9d9a7; text-align:center;">Explore forest rights claims visually on an interactive map.</p>
    </article>
    <article class="card" tabindex="0" aria-describedby="dashboard-desc">
      <div class="card-icon" aria-hidden="true">📊</div>
      <h3 class="card-title">Dashboard Preview</h3>
      <p id="dashboard-desc" style="font-size:0.9rem; color:#c9d9a7; text-align:center;">View detailed charts and progress metrics for FRA implementation.</p>
    </article>
    <article class="card" tabindex="0" aria-describedby="impact-desc">
      <div class="card-icon" aria-hidden="true">🌱</div>
      <h3 class="card-title">Impact</h3>
      <p id="impact-desc" style="font-size:0.9rem; color:#c9d9a7; text-align:center;">See the positive impact of FRA Atlas on forest communities.</p>
    </article>
  </section>

  <script>
    // Simple smooth text appearance animation using JS (like Framer Motion)
    // Already done with CSS animation fadeInUp on hero text.
    // For buttons and cards, no extra JS needed as CSS handles hover animations.

    // Optional: Add keyboard focus visible style for accessibility
    document.body.addEventListener('keydown', (e) => {
      if (e.key === 'Tab') {
        document.body.classList.add('user-is-tabbing');
      }
    });
    document.body.addEventListener('mousedown', () => {
      document.body.classList.remove('user-is-tabbing');
    });
  </script>
</body>
</html>
